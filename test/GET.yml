config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 150
scenarios:
  - flow:
    - loop:
      - get:
          url: '/api/fetchRestaurant/{{$randomNumber(1,10000000)}}'
      count: 5
